const car = {
    brand: "BMW",
    model: "M4"
};

const {brand} = car;
const {model} = car;

console.log(brand);
console.log(model);

const nums = [10, 20, 30];

const[first, second, third] = nums;
console.log(first);
console.log(third);
